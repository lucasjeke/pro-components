{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build",
  "outputDirectory": "docs/dist",
  "rootDirectory": ".",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
